apiVersion: apps/v1
kind: Deployment
metadata:
  name: dogs-deployment
  labels:
    app: dogs
spec:
  selector:
    matchLabels:
      app: dogs
  template:
    metadata:
      labels:
        app: dogs
    spec:
      containers:
      - name: dogs-container
        image: xyz
#        resources:
 #         requests:
#            cpu: 100m
#            memory: 100Mi
#        env:
#        - name: GET_HOSTS_FROM
#          value: dns
        ports:
        - containerPort: 80